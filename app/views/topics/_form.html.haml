= simple_form_for(@topic) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.association :user
    = f.association :forum
    = f.input :messages_count

  .form-actions
    = f.button :submit
